/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Portafolio',
  images: {
    unoptimized: true,
  },
  assetPrefix: '/Portafolio/',
}

module.exports = nextConfig

